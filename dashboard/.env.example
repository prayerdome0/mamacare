# Copy to .env.local for live mode. Without these variables the dashboard
# runs in demo mode (deterministic sample data, no real patient data).
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
